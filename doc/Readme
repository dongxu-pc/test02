   本程序是关于sstable数据库文件批量写入的问题：
   sstable数据文件格式采用的是levelDB数据库的实现方式，有关数据文件格式详情请参看table_format。
   本数据文件的批量写入采用GPU进行加速其目的在于加速数据的写入过程，减轻CPU对数据更新的代价，这一程序，
将直接实现sstable数据库文件的写入，同时，使数据库不用再compact数据文件这会增加数据库的运行效率。
   采用技术